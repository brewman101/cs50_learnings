# This is an example using the len function to count the number of items in a list
# And then referencing their position in the list.

students=["John","Sarah", "Tom"]

for i in range(len(students)):
    print(students[i])